package com.thorgil.gambler.core

enum class DiceRollGameType {
    /**
     * With this game, if a member rolls an even number they win and so double their current
     * point balance and if they roll an odd number they get lose and so get
     * nothing.
     */
    DOUBLE_POINTS_ON_EVEN
}